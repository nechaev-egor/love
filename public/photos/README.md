# Photos for cards

Put your photos here (JPG, PNG, WebP) and add their names to `manifest.json`.

Example manifest.json:
```json
["1.jpg", "2.jpg", "3.jpg", "photo.png"]
```

If the manifest is empty or the folder has no photos — colored placeholders are shown.
